meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/user/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "manuela@gmail.com",
    "password": "Admin@1234"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("token", data.accessToken);
}

settings {
  encodeUrl: true
  timeout: 0
}

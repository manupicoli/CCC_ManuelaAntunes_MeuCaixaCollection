meta {
  name: Update category
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/v1/category/:id
  body: json
  auth: bearer
}

params:path {
  id: 9
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Materiais",
    "description": "Compra de cerâmicas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
